# Root env — used by docker-compose for the Postgres container.
# Copy to .env and adjust if needed; defaults work out of the box for local dev.
DB_NAME=text2sql
DB_USER=text2sql
DB_PASSWORD=text2sql
DB_PORT=5432
